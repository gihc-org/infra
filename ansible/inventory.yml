all:
  hosts:
    gihc_vps:
      ansible_host: 65.109.233.92
      ansible_user: root
      ansible_ssh_private_key_file: ~/.ssh/id_ed25519.hetzner
      # Fixed private IP on the Hetzner network (tofu/main.tf attaches the
      # server at cidrhost(subnet_ip_range, 1) — the first host in the subnet)
      k3s_node_ip: 10.0.1.1
